import mysql.connector

db = mysql.connector.connect(
    host="localhost",
    user="root",
    password="",
    database="dbmahasiswa"   # atau database mahasiswa kamu
)

cursor = db.cursor()

cursor.execute("SELECT * FROM mahasiswa")

data = cursor.fetchall()

for row in data:
    print(row)

cursor.close()
db.close()
